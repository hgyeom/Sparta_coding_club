// 조건부 실행
let x = 10;

// and조건 (&&)
// (x > 0) && console.log("x는 양수입니다.");

// or조건 (||)
// 삼항 연산자와 단축평가
let y; // y = undefined
let z = y || 20; // z = y 이거나 20. y가 undefined이기 때문에 20이 할당된다.
console.log(z);